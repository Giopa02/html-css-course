document.getElementById("valider-btn").addEventListener("click", function () {
    // Réinitialiser les champs du formulaire
    document.getElementById("renseignement-form").reset();

    // Affiche un message pour l'utilisateur (optionnel)
    alert("Merci ! Les informations ont été réinitialisées.");
});